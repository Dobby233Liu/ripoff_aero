didn't bother changing the filenames lmao.

Also because of this, I found out that Kristal DOES take sprite frames 
that start with "_0" (at least for getFrames()/getFramesOrTexture()), which is neat ig.